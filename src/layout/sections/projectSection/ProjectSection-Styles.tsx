import styled from "styled-components";

const ProjectSection = styled.section
    `
        max-width: 1200px;
        margin: 0 auto 80px auto;
        padding: 0 30px;
    `
export const S = {
    ProjectSection,
}